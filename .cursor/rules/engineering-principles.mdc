---
description: 工程协作四原则（执行约束）
globs:
alwaysApply: true
---

# 工程协作四原则（Cursor 约束）

## 1) 先想后写（Think Before Coding）

- 动手前先明确假设；有不确定项先提问，不要自行假定。
- 若存在多种理解，先列出选项与取舍，再执行。
- 若有更简单方案，优先提出并说明理由。
- 遇到信息不足时，先澄清再继续实现。

## 2) 能简则简（Simplicity First）

- 用最少代码解决当前问题，不做未被请求的扩展。
- 不为一次性逻辑增加额外抽象层。
- 不预先增加“可配置性/灵活性”。
- 不为极低概率场景添加复杂分支。
- 若实现明显过度，主动回退到更小可行方案。

## 3) 精准修改（Surgical Changes）

- 只修改与当前任务直接相关的代码与文件。
- 不顺手优化无关代码、注释或格式。
- 不重构无问题逻辑（除非任务明确要求）。
- 保持原有代码风格一致。
- 仅清理由本次改动引入的冗余（import、变量、函数）。

## 4) 目标驱动执行（Goal-Driven Execution）

- 先把请求转成可验证目标，再开始实现。
- 涉及缺陷修复：优先构造可复现路径，再修复，再验证。
- 涉及重构：确保重构前后行为一致，并通过检查。
- 多步骤任务需给出简要步骤与每步验证点。
- 只有验证通过后，才视为任务完成。

# 远程访问

当你想从其他设备（如手机、平板或独立的桌面应用）连接到 T3 Code 服务器时，请使用此功能。

## 推荐设置

使用可信的私有网络将你的设备互联，例如 Tailnet。

这样可以获得：

- 稳定的连接地址
- 网络层的传输安全
- 比将服务器暴露在公共互联网上更安全

## 启用网络访问

有两种方式可以暴露你的服务器以供远程连接：通过桌面应用或通过命令行。

### 方式一：桌面应用

如果你已经在运行桌面应用，并希望让其他设备可以访问：

1. 打开 **设置** → **连接**。
2. 在 **管理本地后端** 下，开启 **网络访问**。这将重启应用并在所有网络接口上运行后端。
3. 设置面板将显示服务器可访问的地址（例如 `http://192.168.x.y:3773`）。
4. 使用 **创建链接** 生成一个配对链接，你可以将其分享给其他设备。

### 方式二：无头服务器（命令行）

当你想在没有图形界面的情况下运行服务器时使用此方式，例如通过 SSH 在远程机器上运行。

使用 `t3 serve` 启动服务器。

```bash
npx t3 serve --host "$(tailscale ip -4)"
```

`t3 serve` 启动服务器时不会打开浏览器，并会打印：

- 连接字符串
- 配对令牌
- 配对 URL
- 配对 URL 的二维码

然后，你可以通过以下任一方式从其他设备连接：

- 在手机上扫描二维码
- 在桌面应用中输入完整的配对 URL
- 在桌面应用中分别输入主机和令牌

使用 `t3 serve --help` 查看完整的参数参考。它支持与普通服务器命令相同的常规启动选项，包括可选的 `cwd` 参数。

> 注意
> 图形界面目前不支持在远程环境中添加项目。
> 目前请在服务器机器上使用 `t3 project ...` 命令代替。
> 完整的远程项目管理图形界面支持即将推出。

## 配对工作原理

远程设备不需要预先持有长期有效的密钥。

相反：

1. `t3 serve` 生成一个一次性的所有者配对令牌。
2. 远程设备使用该令牌与服务器交换。
3. 服务器为该设备创建一个已认证的会话。

配对后，后续访问基于会话进行。你不需要重复使用原始令牌，除非你要配对新设备。

## 后续访问管理

在初始配对流程完成后，使用 `t3 auth` 管理访问权限。

典型用途：

- 签发额外的配对凭据
- 查看活跃会话
- 撤销旧的配对链接或会话

使用 `t3 auth --help` 和嵌套子命令的帮助页面查看完整参考。

## 安全注意事项

- 将配对 URL 和配对令牌视为密码一样重要。
- 优先将 `--host` 绑定到可信的私有地址，例如 Tailnet IP，而不是广泛暴露服务器。
- 任何持有有效配对凭据的人都可以创建会话，直到该凭据过期或被撤销。
- 使用 `t3 auth` 撤销你不再信任的凭据或会话。
